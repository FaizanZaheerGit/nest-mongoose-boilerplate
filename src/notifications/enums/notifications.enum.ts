export enum NotificationCategories {
  SYSTEM = 'system',
  ALERT = 'alert',
  PROMOTION = 'promotion',
  REMINDER = 'reminder',
}

export enum NotificationChannels {
  IN_APP = 'in-app',
  PUSH = 'push',
}
